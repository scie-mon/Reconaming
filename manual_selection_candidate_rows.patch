diff --git a/bin/select_representative_isoforms.py b/bin/select_representative_isoforms.py
index 0000000..0000000 100755
--- a/bin/select_representative_isoforms.py
+++ b/bin/select_representative_isoforms.py
@@ -111,7 +111,6 @@ def main(args):
 
         selected = {}
         unresolved_marker_groups = set()
-        candidate_rows_present = False
 
         for line_number, supplied in enumerate(supplied_rows, start=2):
             gene_id = (supplied.get('gene_id') or '').strip()
             transcript_id = (supplied.get('transcript_id') or '').strip()
             context = f'line {line_number}'
 
-            if not gene_id and transcript_id:
-                candidate_rows_present = True
-                validation_rows.append(['warning', context, '', '', transcript_id, '', 'candidate_row_requires_removal', 'Candidate rows are template hints and are not selections.'])
-                continue
-            if not gene_id and not transcript_id:
-                errors.append(f'{context}: both gene_id and transcript_id are empty.')
-                validation_rows.append(['error', context, '', '', '', '', 'empty_row', 'Both fields are empty.'])
+            if not gene_id:
+                validation_rows.append(['info', context, '', '', transcript_id, '', 'ignored_candidate_row', 'Row has an empty gene_id and is ignored.'])
                 continue
             if gene_id and not transcript_id:
                 unresolved_marker_groups.add(gene_id)
                 validation_rows.append(['warning', context, gene_id, '', '', '', 'unresolved_marker', 'Gene has no selected transcript.'])
@@ -159,7 +157,7 @@ def main(args):
             chosen[gene_key] = row
             modes[gene_key] = 'manual'
 
-        needs_template = bool(errors or missing or unresolved_marker_groups or candidate_rows_present)
+        needs_template = bool(errors or missing or unresolved_marker_groups)
         if needs_template:
             status = 'manual_selection_required'
             template_rows = []
diff --git a/modules/local/require_complete_manual_selection.nf b/modules/local/require_complete_manual_selection.nf
index 0000000..0000000 100644
--- a/modules/local/require_complete_manual_selection.nf
+++ b/modules/local/require_complete_manual_selection.nf
@@ -22,8 +22,8 @@ Manual isoform selection is incomplete.
 
 For every gene with more than one isoform, retain exactly one completed:
   gene_id<TAB>transcript_id
-row. Copy the desired candidate transcript into that gene's row and delete
-all blank-gene candidate rows. Then rerun with the revised file as:
+row. Copy the desired candidate transcript into that gene's row. Rows with
+an empty gene_id are ignored and may remain in the table. Then rerun as:
   --selection_table path/to/revised_selection.tsv
 and add -resume.
 MESSAGE
